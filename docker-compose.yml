version: '3.8'

services:
  listener:
    build: .
    container_name: submission-sequencer-listener
    env_file:
      - .env
    ports:
      - "8001:8001"
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: submission-sequencer-redis
    ports:
      - "6380:6379"